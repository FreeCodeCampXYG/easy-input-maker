# 进展日志（最新记录置顶）

> 这里只记录适合公开的结果、产出路径和下一步，不记录内部讨论、私密信息或未经整理的执行过程。

## 2026-08-25 · Host Action v1 学员起点建立 · Codex
- 做了什么：从已经通过完整验证的 Maker 公共基线建立课程分支，只移除 Host Action 的配置解析、事件、共享编码、USB／BLE发送、能力声明和答案型测试；BLE 持久化修复及全部无关公共能力继续保留。starter 重新发现并通过 57/57 项宿主测试，ESP-IDF 5.5.5 默认构建通过。
- 为什么这么做：让学员按课程提示词逐步实现“打开应用”，同时避免退回旧固件后把 BLE 配置问题误当成课程功能问题。
- 怎么理解：完整 `main` 与 starter 的有意差异只有课程目标及其直接证据；OTA、签名和部署始终不属于 Maker 课程。缺少 Host Action 是起点合同，不是回归。
- 产出（路径）：课程边界见 `AGENTS.md` 与 `README.md`；冻结合同见 `flow/tasks/T02-Host-Action-v1-固定兼容协议.md`；学员推进节点见 `flow/plan.md` 和对应任务卡。
- 问题→解决：移除能力字段后，一个 512 字节回退测试样本不再真实超限；把输入改为仍在公开预算内但能触发超限的 40 字节 firmware 边界，继续证明 current power 保留、recent cycle 只在超限时省略。
- 下一步：从 T03 开始一次完成一个节点；每步先补宿主测试，再实现和回归。未经明确授权不烧录、不改硬件合同，也不引入 OTA。
