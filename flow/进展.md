# 进展日志（最新记录置顶）

> 这里只记录适合公开的结果、产出路径和下一步，不记录内部讨论、私密信息或未经整理的执行过程。

## 2026-08-23 · Host Action v1 本地原子提交完成、尚未推送 · Codex
- 做了什么：提交前重新配置并完整编译宿主测试，实际执行 59 项且 59/59 通过；随后把 Host Action v1 的生产实现、宿主测试和公开协作记录共 33 个文件组成单一提交 `feat: add Host Action v1 support`。提交级 `diff --check` 通过，工作树除被忽略的生成目录外已干净；未推送、未再次构建固件、烧录或访问设备。
- 为什么这么做：把已经完成合同核对、软件验收、边界审计和写入证据的同一功能闭合成可独立回退的本地提交，同时避免把生成物或旧试跑 stash 混入 Git 历史。
- 怎么理解：`main` 当前相对 `origin/main` 领先 1 个提交，远端仍停在 `34087cd`；提交成功不改变设备验证状态。最近一次固件已写入，但在使用者确认正常断电启动前，仍是 `NORMAL_POWER_CYCLE_CONFIRMED=NO`、`POST_FLASH_READY=UNKNOWN`、`FUNCTION_VERIFIED=NO`。
- 产出（路径）：本地提交 `feat: add Host Action v1 support`；协议与纯逻辑位于 `components/keyboard/`，平台接入位于 `main/app_main.cpp`、`main/platform/usb_hid.cpp`、`main/platform/ble_hid.cpp`，宿主覆盖位于 `host_test/`，完整合同与证据位于 `flow/tasks/T02-*` 至 `T10-*`。
- 问题→解决：工作区同时包含 20 个 tracked 修改、13 个 untracked 文件、3 个 ignored 生成目录和 1 个旧 stash；通过逐路径显式暂存、暂存区敏感信息扫描、33 文件提交对象复核和完整宿主回归，确认提交只包含 Host Action v1 主线。旧 stash 保持未应用、未删除。
- 下一步：如需公开该提交，先由使用者另行授权 push；真机侧独立完成板上电源开关完全断电、保持 BOOT 不动正常开机及后续功能验证，不能用提交或 59/59 宿主测试代替。

## 2026-08-22 · Host Action v1 固件写入完成、等待正常断电恢复 · Codex
- 做了什么：收到使用者针对已验明目标设备的精确烧录确认后，fresh 扫描仍只有一个候选，并再次只读确认是同一 ESP32-S3／同一 MAC；随后使用项目锁定的 ESP-IDF v5.5.5 和显式端口执行正常项目 `flash`，退出码 0。bootloader、既有分区表和 `0x190470` 字节应用镜像均写入完成且数据哈希校验通过；未执行 `erase_flash` 或整片擦除。烧录端口仍存在，因此按本轮人工短按 BOOT 的 `manual` 历史建立了烧录后检查 session，session 前置身份再次匹配。
- 为什么这么做：写入授权只能绑定到最后一次 fresh MAC 比对通过的设备；写入后还必须在最终物理恢复前保留同设备证据，避免把 flash 退出 0 误当成应用已经正常启动。
- 怎么理解：当前 `DEVICE_CONFIRMED=YES`、`FLASH_WRITTEN=YES`，只证明指定三段固件已成功写入同一目标。开发板此前由人工 BOOT 进入下载模式，仍需用板上电源开关完整关机再正常开机；在使用者确认这次电源循环前，`NORMAL_POWER_CYCLE_CONFIRMED=NO`、`POST_FLASH_READY=UNKNOWN`、`FUNCTION_VERIFIED=NO`。
- 产出（路径）：写入使用被 Git 忽略的 `build/bootloader/bootloader.bin`、`build/partition_table/partition-table.bin` 与 `build/easy_input_keyboard.bin`；应用 SHA-256 仍为 `1c355687d5a17ba8482ac1d7c3153d763f702fcd5c59fec768239fd7f9d16a93`。本节点没有修改生产代码、测试、GPIO、BOOT、GPIO8、Flash 分区、HID／GATT、设备身份或 App 映射。
- 问题→解决：正常项目烧录的自动连接一次成功，无需再次操作 BOOT，也没有发生写入中断或重试。工具完成 hard reset 后下载端口仍存在，因此依规建立 session；没有为了 session 重新烧录、额外复位或执行整片擦除。完整 MAC、端口、本机路径、session token 和原始烧录日志均未写入项目记录。
- 下一步：使用者用板上电源开关完全关机，再不按 BOOT 正常开机；如有电池，只拔 USB 不算完全断电。确认后不再运行 identify-device 或 esptool，只做一次不写设备的操作系统 USB／HID 枚举，查找 `EasyInput AI` 与 VID/PID `0x303A/0x1006`，再完成最终状态和公开交接。

## 2026-08-22 · Host Action v1 目标设备验身通过、等待精确烧录确认 · Codex
- 做了什么：使用者按当前板正确规则在开机状态短按并松开一次 BOOT 后，fresh 扫描只发现一个下载端口；在项目锁定的 ESP-IDF v5.5.5 环境中只读识别为 ESP32-S3，设备身份与临时授权卡一致，因此 `DEVICE_CONFIRMED=YES`。随后重新读取 `build/flash_args`、三段镜像大小和 SHA-256，并再次确认应用 ELF 干跑为 `no work to do`；已向使用者签发包含完整身份的临时写入授权卡，但尚未收到精确确认，未烧录、擦除或写设备。
- 为什么这么做：端口名会随 USB 重枚举变化，不能代替芯片和 MAC 身份；同时授权必须对应当前已验收、未过期的具体构建产物和明确写入范围，不能只凭“板子已连上”或 T10 静态审计直接烧录。
- 怎么理解：计划执行正常项目 `flash`，写入 bootloader、既有分区表和应用三段，不执行整片擦除；应用镜像为 `0x190470` 字节，SHA-256 为 `1c355687d5a17ba8482ac1d7c3153d763f702fcd5c59fec768239fd7f9d16a93`。当前只证明目标设备和本地待写产物已经对齐，`FLASH_WRITTEN`、`NORMAL_POWER_CYCLE_CONFIRMED` 与功能验证仍均未完成。
- 产出（路径）：待写产物仍为被 Git 忽略的 `build/bootloader/bootloader.bin`、`build/partition_table/partition-table.bin` 和 `build/easy_input_keyboard.bin`；烧录前全量边界依据仍见 `flow/tasks/T10-Host-Action-v1-烧录前只读修改范围审计.md`。本节点没有修改生产代码、测试、构建配置、硬件配置或设备身份。
- 问题→解决：正常 HID 模式没有 CDC 端口，不能做芯片与 MAC 验身；按当前实板规则进入下载模式后出现唯一候选，使用只读识别取得 ESP32-S3 和 MAC。完整身份仅临时显示在当前对话授权卡，项目记录不保留完整或部分 MAC，未创建设备登记。
- 下一步：等待使用者原样输入临时授权卡给出的唯一确认句；任何其他回复均不授权。收到后先 fresh 重扫并重验同一 MAC，身份一致才使用显式端口执行 ESP-IDF v5.5.5 正常项目烧录；身份变化、不可读或多设备歧义立即停止。

## 2026-08-22 · Host Action v1 正常模式 HID 已见、等待进入下载模式验身 · Codex
- 做了什么：使用者确认开发板已连接并开机后，再次只读核对工作区连续性、串口和 USB 枚举；未出现新的非 `flow/` 固件变化。串口仍为 `PORT_COUNT=0`，但操作系统 USB 树已观察到产品名 `EasyInput AI`、VID/PID `0x303A/0x1006`，未读取或保存完整序列号与 USB 拓扑。未调用 esptool，未烧录、擦除或写设备。
- 为什么这么做：正常应用只有 HID、没有 CDC，单靠串口扫描无法区分“板子未连接”和“板子正常以 HID 枚举”；先用最小 USB 身份字段交叉确认，才能在不泄露设备唯一信息的前提下选择当前板正确的下载模式入口。
- 怎么理解：预期 HID 已出现，只证明电脑看见当前工程约定的正常模式 USB 身份，不证明芯片型号、MAC、固件版本或 Host Action 功能。因为尚无下载端口，`DEVICE_CONFIRMED` 仍为 NO，也不能生成写入授权卡或执行烧录。
- 产出（路径）：烧录前静态安全基线仍见 `flow/tasks/T10-Host-Action-v1-烧录前只读修改范围审计.md`；本节点仅增加本条公开交接，没有修改生产代码、测试、构建配置、硬件配置或设备身份。
- 问题→解决：`system_profiler` 在当前执行环境返回空 USB 列表，不能据此断言系统未见设备；改用同样只读的 I/O Registry，并只筛出产品名和 VID/PID，确认预期 HID 存在。依据 easyinput-board-cy 的当前实板规则，唯一下一实体动作是开机状态短按并松开一次 BOOT，不采用按住 BOOT 上电或不存在的 RESET。
- 下一步：等待使用者完成一次“保持开发板开机，短按并松开 BOOT”；随后 fresh 重扫端口。只有唯一端口且只读识别为 ESP32-S3、完整 MAC 可读，才返回写入授权卡和由 MAC 后四位生成的唯一确认句；收到精确确认前仍不得写设备。

## 2026-08-22 · Host Action v1 烧录前设备验身等待连接 · Codex
- 做了什么：承接 T10 的 `SAFE_TO_REQUEST_FLASH = YES`，重新核对审计后的源码、测试、构建配置、硬件配置和设备身份差异，未发现新的非 `flow/` 固件相关变化；锁定 ESP-IDF v5.5.5，确认目标仍为 `v2`／`esp32s3`，应用 ELF 专项目标干跑为 `no work to do`，且没有生产源码或构建配置比已验收 ELF 更新。随后只读扫描串口与操作系统 USB 枚举，均未发现当前连接的候选设备；未运行烧录、擦除或其他设备写入命令。
- 为什么这么做：T10 只允许提出烧录请求，真正写入前仍须证明工作区、构建产物和目标设备连续一致，并由使用者根据完整设备身份亲自确认，不能把已有审计或未知端口直接当成烧录授权。
- 怎么理解：默认 Ninja 干跑列出的 bootloader 外部工程收尾和分区尺寸检查属于缺少 stamp 的常规步骤；对应用 ELF 单独干跑无待执行工作，生产输入也没有更新，因此不触发 `REBUILD_REQUIRED`。当前 `PORT_COUNT=0` 只表示尚不能读取 ESP32-S3 与 MAC、不能签发写入授权卡；它不等于固件失败，也不改变 T10 的静态审计结论。
- 产出（路径）：既有全量范围与安全结论仍见 `flow/tasks/T10-Host-Action-v1-烧录前只读修改范围审计.md`；待写应用镜像仍为被 Git 忽略的 `build/easy_input_keyboard.bin`，本节点没有新增代码、测试、硬件配置或设备身份产出。
- 问题→解决：正常 Maker 应用只有 HID、没有 CDC 串口，首次扫描得到 0 个端口；继续用不保存序列号和 USB 拓扑的系统级只读枚举交叉检查，仍未观察到设备，因此没有猜端口、没有调用 esptool，也没有提前要求进入下载模式。
- 下一步：使用者用可传输数据的 USB 线连接开发板并打开板上电源开关、暂不按 BOOT；随后重新扫描。只有唯一候选且只读识别为 ESP32-S3、完整 MAC 可读时，才生成带显式端口和精确确认句的写入授权卡；收到原样确认前仍不得烧录。

## 2026-08-22 · Host Action v1 烧录前只读修改范围审计完成 · Codex
- 做了什么：以课程起点和当前共同指向的 `main@34087cd` 为可靠基线，审计到当前工作区的全部 staged、unstaged、untracked 与 ignored 内容；实际读取每个 diff／新文件，把写入前 32 个变化文件归为 12 个 Host Action／状态兼容生产文件、10 个宿主测试文件和 10 个 `flow/` 记录，并核对 `build/`、`build-host/` 两个忽略生成目录。未修改生产代码、配置或测试，未访问设备、串口或 App，未烧录。
- 为什么这么做：上一节点只增加 `flow/` 不代表整个工作区没有代码差异；烧录请求前必须从课程起点审清累计改动，避免把局部增量检查误当完整范围证明。
- 怎么理解：板级 GPIO／BOOT／GPIO8、v2／esp32s3／16 MB Flash／分区、USB／BLE 身份／描述符／GATT／地址策略、Host Action 冻结字节合同、应用映射边界、日志隐私、无关功能和生成物忽略共八项均为 PASS；没有课程前非 `flow/` 改动、无关改动或无法解释项。因此 `SAFE_TO_REQUEST_FLASH = YES` 只表示可以另行请求烧录授权，不是已经授权或验证真机。
- 产出（路径）：完整命令、全量文件分类、身份片段哈希、合同与禁止修改矩阵见 `flow/tasks/T10-Host-Action-v1-烧录前只读修改范围审计.md`。
- 问题→解决：easyinput-board-cy 静态扫描因未选择条件编译分支给出 2 个 WARN、0 FAIL；没有把 WARN 猜成 PASS，而是用 `HEAD` 可靠基线零差异、v2 编译定义和当前引脚声明逐项补证。未跟踪中文路径的首次行数枚举受 Git 引号转义影响，改用 NUL 分隔重新枚举后 12 个文件全部读取和归类。
- 下一步：如需烧录，先在新的明确授权节点确认目标设备与写入范围；本审计不替代设备验身、烧录、启动日志、USB／BLE、App 0.1.26 或实板功能验证。

## 2026-08-22 · Host Action v1 软件侧总验收完成 · Codex
- 做了什么：不复用上一轮绿灯作为本轮结果，重新配置宿主工程并先读取 CTest 清单，核对课程起点 56 个与 Host Action 新增的 3 个测试目标；本轮实际发现、执行和通过均为 59，失败 0。随后激活项目锁定的 ESP-IDF v5.5.5，对默认 `v2`／`esp32s3` 完成构建；未识别设备、读取串口、烧录或运行 App。
- 为什么这么做：在 Host Action 的合同、纯逻辑、八键、发送和能力节点都完成后，用一次独立的全量重新运行确认所有宿主测试仍被 CTest 注册并执行，同时确认当前工作区能够生成目标固件。
- 怎么理解：`59/59` 只证明宿主逻辑与源码合同通过；`Project build complete` 只证明能生成固件。两者都不等于已经烧录，也不证明 USB／BLE、状态发布或 EasyInput App 0.1.26 在真机上工作。
- 产出（路径）：完整 59 项清单、命令、退出码、增量编译文件、警告和边界证据见 `flow/tasks/T09-Host-Action-v1-软件侧总验收.md`；固件产物为被 Git 忽略的 `build/easy_input_keyboard.bin` 和 `build/easy_input_keyboard.elf`。
- 问题→解决：普通 shell 没有直接暴露 `cmake`／`ctest`，复用 ESP-IDF 工具目录中的 CMake 3.30.2 后按工程约定完成配置、清单和全量执行，没有把命令查找问题冒充代码失败。宿主测试、环境、依赖、编译和链接均无失败，因此本轮没有代码修复；应用镜像为 `0x190470` 字节，3 MiB App 分区剩余 `0x16fb90` 字节（48%），构建输出 warning 为 0。
- 下一步：如需继续，必须在单独明确授权下完成烧录、串口和 EasyInput App 0.1.26／USB／BLE 真机验证；当前仍不得把软件测试或构建成功表述成实板功能通过。

## 2026-08-22 · Host Action v1 能力声明与 BLE 状态预算完成 · Codex
- 做了什么：在 T02／T03／T04／T06 基线上，把唯一布尔能力 `"host_action_v1": true` 加入完整、紧凑、speaker probe、确认、battery 和共享 BLE fallback 的 `capabilities` 对象；把既有非 battery BLE 最终附加编码等价抽到共享纯逻辑入口，补齐逐变体能力、最终字节数、speaker 指标和 power/cycle 回退宿主测试。未修改 Host Action 发送合同、USB 路由、HID／GATT 或硬件配置，未运行 ESP-IDF 构建和烧录。
- 为什么这么做：让 EasyInput App 能通过状态 JSON 明确判断固件支持 Host Action v1，并用真实序列化结果守住 BLE 512 字节边界，而不是把能力混入动作 payload 或只相信源码常量。
- 怎么理解：状态响应仍是 kind `0x04`，Host Action 仍是 Report ID `0x11`／kind `0x05`；能力只描述“固件会不会”，不携带 UUID。speaker probe 保留全部指标，仅其 firmware 内容上限为 16；含 recent cycle 的状态能放下就完整保留，超限时保留 current power 的 `mode/inactive_ms/deep_sleep_block/last_wake`，只先省略四个 cycle 字段。
- 产出（路径）：实现位于 `components/keyboard/include/keyboard/config_status.h`、`components/keyboard/include/keyboard/ble_status_wire.h`、`components/keyboard/src/config_status.cpp`、`main/platform/ble_hid.cpp`；测试位于 `host_test/config_status_tests.cpp`、`host_test/host_action_capability_status_tests.cpp`、`host_test/firmware_source_contract_tests.cpp`；完整证据见 `flow/tasks/T08-Host-Action-v1-能力声明与BLE状态预算.md`。
- 问题→解决：能力红测先在缺少字段处按预期失败；加入字段后，周期状态和 speaker probe 暴露真实预算压力。按批准规则增加无周期中间回退、把 speaker probe 专用 firmware 预算收紧为 16，并对 BLE 最终编码做宿主执行验证后，定向 6/6、完整宿主 59/59 通过。最终变体为 full 408、compact 483、speaker probe 512、confirmation 444、battery 512、fallback 241 字节，最大 512/512，剩余 0。
- 下一步：在单独授权节点运行 ESP-IDF 5.5.5 默认构建，随后再决定是否使用 EasyInput App 0.1.26 做能力识别与真机状态读取；烧录、串口和 USB／BLE 实板发送仍需单独明确授权。

## 2026-08-22 · Host Action v1 Vendor HID 单通道发送接入完成 · Codex
- 做了什么：在 T02／T03／T04 基线上，把 `FirmwareEventKind::HostAction` 接入 `dispatch_firmware_event()` 和既有 USB／BLE App Command 队列；两侧都先调用共享 `encode_host_action_v1()`，没有复制 Host Action kind 或长度常量。同步增加 payload 余量归零、事件分发源码合同和 USB 优先路由测试；未增加能力声明，未改描述符、GATT 或硬件配置，未烧录。
- 为什么这么做：让已经通过纯逻辑和八键测试的事件真正到达现有 Vendor HID 发送入口，同时保持 v1 字节合同只有一个编码事实源，并保持原有单通道所有权边界。
- 怎么理解：按键按下产生一次完整 Host Action 事件；物理事件到达时若 USB endpoint 存在，只向该 USB epoch 入队，失败也直接结束，不转投 BLE；没有 USB owner 时才向当前 BLE owner 入队。两侧最终都发送 Report ID `0x11` 的同一 63 字节容器，其中头为 `0x05/0/1/36`、UUID 位于 `[4..39]`、`[40..62]` 为零。
- 产出（路径）：运行接入位于 `main/app_main.cpp`、`main/platform/usb_hid.cpp`、`main/platform/ble_hid.cpp`；宿主合同增量位于 `host_test/host_action_protocol_tests.cpp`、`host_test/transport_routing_tests.cpp`、`host_test/firmware_source_contract_tests.cpp`；完整证据见 `flow/tasks/T06-Host-Action-v1-Vendor-HID-发送接入.md`。
- 问题→解决：测试先在缺少运行分发分支处按预期失败；补齐三个最小 switch 分支后，Host Action 定向 8/8、完整宿主 58/58 通过，ESP-IDF v5.5.5／ESP32-S3 默认构建完成。平台适配无法由宿主直接执行，因此以共享逻辑测试、平台源码合同与真实交叉编译分开表述，不虚构传输或实板结果。
- 下一步：用 EasyInput App 0.1.26 同步真机 UUID 后，在明确烧录授权的独立节点验证 USB-only、BLE-only、双连接 USB 优先以及已选通道失败不补发；第 08 步再增加能力声明并检查 BLE 状态不超过 512 字节。

## 2026-08-22 · Host Action v1 八个实体主按键配置覆盖完成 · Codex
- 做了什么：读取 T02 冻结合同与 T03 纯逻辑交接后，确认 `config_payload.cpp` 用同一张 KEY1—KEY8 表循环调用 `parse_binding()`；因此未改生产逻辑，只新增参数化宿主测试，逐键验证完整 `host_action:` 前缀、按下一次 Host Action、松开 `None` 和其余七键不串位。同步回归复制、粘贴、快捷键、固定文字及全部宿主测试；未改 `main/platform`、USB／BLE、能力声明或硬件配置，未烧录。
- 为什么这么做：把“通用解析看起来能覆盖八键”提升为八个槽位逐项执行的自动化证据，同时避免为制造代码差异而复制八套解析或改写已正确架构。
- 怎么理解：KEY1—KEY8 的区别只在 `InputId` 落位，动作解析与事件生成完全共用。测试对每个目标键执行一次 Pressed／Released 周期，统计 Host Action 事件必须恰好为 1；固定示例 UUID 只存在 `host_test/`，默认 Keymap 与生产配置没有该值。
- 产出（路径）：新增 `host_test/host_action_key_bindings_tests.cpp` 并在 `host_test/CMakeLists.txt` 注册；八键矩阵、旧动作证据、命令和未验证项见 `flow/tasks/T04-Host-Action-v1-八键配置覆盖.md`。
- 问题→解决：检查发现能力已经由通用循环提供，不存在需要修复的逐键分支；以新增覆盖测试替代生产代码改动。定向 5/5、完整宿主 58/58 通过，且本轮前后配置／Keymap 生产文件哈希不变。
- 下一步：保持当前纯逻辑边界；如后续获明确授权，再处理 NVS、`main/` 与 USB／BLE 传输适配。ESP-IDF 构建、App 同步、端到端发送、烧录、实板和第 08 步能力声明仍未验证。

## 2026-08-22 · Host Action v1 纯逻辑实现完成 · Codex
- 做了什么：先逐项核对 T02 冻结合同，再先补配置、fail-closed、Keymap 事件和共享报告编码的宿主测试；取得缺少 `host_action_protocol.h` 的预期编译红灯后，最小实现规范小写 UUID 校验、完整 `host_action:<UUID>` 配置保存、按下一次／松开 `None`，以及固定 `0x11`／`0x05`／`0`／`1`／`36` 的共享编码。未修改 `main/`、USB 或 BLE，未构建 ESP-IDF，未烧录。
- 为什么这么做：把冻结合同先落实为可在电脑验证的唯一纯逻辑事实源，使后续传输适配只能复用已经通过测试的字节内容，不能在 USB／BLE 两侧各自解释协议。
- 怎么理解：配置与 Keymap 保留完整前缀；共享编码再次严格校验后只把 36 字节 UUID ASCII 放入 payload `[4..39]`。nil UUID 和任意 version 位只要格式规范就接受；大写、错误长度、错误连字符和非法字符返回 `unknown_action`，`ConfigState` 不替换上一次有效配置。Host Action kind 固定为 `0x05`，测试同时静态确认状态响应仍为 `0x04`。
- 产出（路径）：实现位于 `components/keyboard/include/keyboard/host_action_protocol.h`、`components/keyboard/src/host_action_protocol.cpp`、`config_payload.cpp` 与 `keymap.*`；宿主测试位于 `host_test/host_action_protocol_tests.cpp`、`config_payload_tests.cpp`、`config_state_tests.cpp`、`keymap_tests.cpp`；完整文件与证据清单见 `flow/tasks/T03-Host-Action-v1-纯逻辑实现.md`。
- 问题→解决：普通 shell 没有直接暴露 `cmake`；复用当前 ESP-IDF 工具链中已安装的 CMake 3.30.2 完成同一组项目命令，并按公开边界不把本机 SDK 绝对路径写入项目记录。红灯为测试引用的协议头尚不存在；补齐共享协议层后定向 4/4、完整宿主 57/57 通过。
- 下一步：保持 T02 合同不变；下一节点如获授权再接 `main/` 与 USB／BLE 单通道适配。当前仍未验证 NVS 持久化、ESP-IDF 构建、App 同步、传输字节、烧录或实板；第 08 步能力声明也未实现。

## 2026-08-22 · Host Action v1 固定兼容协议完成冲突核对与归档 · Codex
- 做了什么：先记录 `main@34087cd` 的操作前状态，再只读核对当前 Maker 的 App Command 编号、Report ID、USB／BLE 报告容量、状态响应和运输选择；未发现 kind `0x05` 被占用或固定字段超出容器，因此把使用者给定的 Host Action v1 合同、后续完整改动层和不承担范围写入 `flow/`。本节点未修改代码，未运行测试、构建或烧录，也未读取其他电脑端工程。
- 为什么这么做：先把 App 0.1.26 与 Maker 后续实现必须共同遵守的字节级合同冻结下来，防止后续把 63 字节容器误当 Host Action 数据长度、复用状态 kind `0x04`、自动改写 UUID，或让 USB／BLE 双发。
- 怎么理解：当前公开基线没有 Host Action 是预期起点，不是冲突。现有 `0x11` App Command 负载为 63 字节，头部 `[0..3]` 后有 59 字节数据空间；v1 的 36 字节 UUID 使用 `[4..39]`，`[40..62]` 只是现有容器余量。固件只携带 UUID，不认识本机应用；真实映射由 EasyInput App 本地持有。
- 产出（路径）：完整合同与逐项自检在 `flow/tasks/T02-Host-Action-v1-固定兼容协议.md`；冻结决策在 `flow/decisions.md`；计划入口在 `flow/plan.md`。
- 问题→解决：仓库里存在许多与 HID Usage、键码或测试数据有关的字面量 `0x05`，不能据此判断编号冲突；把检索范围收敛到 App Command kind 常量和编码函数后，确认现有 kind 依次为 `0x01` 固定文字、`0x02` 热键、`0x03` 配置回执、`0x04` 状态响应，`0x05` 未实现。
- 下一步：后续获得开发授权后严格按任务卡补齐配置持久化、Keymap／事件、共享编码、USB／BLE 适配和宿主测试；能力声明 `"host_action_v1": true` 只在第 08 步加入，并同时验证 BLE 状态不超过 512 字节。暂不改代码或烧录。

## 2026-08-22 · KEY2 标准按键发送链路只读追踪完成 · Codex
- 做了什么：未修改代码，以默认 `KEY2 → Return` 为例，只读追踪 App 配置经 USB／BLE 收包、`ConfigReceiver` 拼包校验、`ConfigState` 与 `Keymap` 生效，以及 GPIO47 按下经 `GpioInputScanner`、`handle_input_event()`、`event_for_action()`、HID 快照与运输锁存，最终进入 TinyUSB 或 BLE HIDD 的真实函数链；同时确认松开事件沿同一 owner 发送释放快照。
- 为什么这么做：在后续扩展 Host Action 前先固定现有标准键盘路径，避免把“配置如何形成 Keymap”“固件内部事件”和“USB／BLE 实际发送”混成一个步骤。
- 怎么理解：标准 KEY2 按下会把当前 `Hotkey("Return")` 转为 `HidKeyDown`，再解析为 HID 键码 `0x28`；`HeldKeyboardState` 生成整把键盘的当前快照，`KeyboardTransportLatch` 在 USB 可用时优先锁定 USB，否则锁定 BLE，并把按下与松开绑定到同一连接生命周期。连接中途丢失时不会把剩余状态迁移给新主机。
- 产出（路径）：配置与动作入口位于 `components/keyboard/src/config_receiver.cpp`、`config_payload.cpp`、`config_state.cpp`、`keymap.cpp`；实体输入位于 `main/platform/gpio_keys.cpp`；编排位于 `main/app_main.cpp`；最终发送位于 `main/platform/usb_hid.cpp` 和 `main/platform/ble_hid.cpp`。本轮无代码、测试或构建产出。
- 问题→解决：初看容易把 App 命令分支与标准键盘 HID 分支混淆；选用能够原生解析为 HID 的 KEY2／Return 后，确认它走 `dispatch_held_keyboard_event()`，而不是 App bridge，并继续追到 USB 的 `tud_hid_report()` 与 BLE 的 `easy_input_hidd_dev_input_set_for_owner()`。
- 下一步：若进入 Host Action v1 开发，先以这条标准 HID 链路为对照，明确哪些动作仍应走原生键盘、哪些动作需要独立 App Command 合同，再补失败优先的宿主测试；暂不烧录。

## 2026-08-22 · Host Action 课程复跑软件基线验证完成 · Codex
- 做了什么：未修改代码，按 Maker 默认命令完成宿主配置、编译和全部测试，56／56 通过、0 失败；随后用 `esp-idf-cy` 选定并验证 ESP-IDF v5.5.5，对 `v2`／`esp32s3` 完成默认构建，生成应用镜像 `0x18fd00` 字节，3 MiB App 分区剩余 `0x170300` 字节（48%）；未识别设备、未读串口、未烧录。
- 为什么这么做：在继续任何 Host Action 固件开发前，先取得当前 Maker 基线的纯逻辑与交叉编译证据，避免把已有环境或基线问题误归因于后续功能修改。
- 怎么理解：56／56 只证明当前宿主逻辑测试通过；`Project build complete` 只证明 ESP-IDF 5.5.5 能为 ESP32-S3 生成固件。二者都不能替代 App 同步、烧录、启动日志或实板行为。
- 产出（路径）：宿主产物位于被忽略的 `build-host/`；默认固件位于被忽略的 `build/easy_input_keyboard.bin`，构建元数据位于 `build/project_description.json`；本轮没有新增版本控制代码产出。
- 问题→解决：普通 shell 最初缺少 `cmake`，环境体检又发现两套 ESP-IDF 并拒绝自动猜选；按项目锁定版本显式激活 v5.5.5 后，环境 `READY=yes`，宿主测试与默认构建均成功。组件注册表联网检查失败仅产生 notice，本地锁定依赖仍完整完成构建。
- 下一步：等待下一项明确开发授权；如继续 Host Action v1，先固定 App 0.1.26 与 Maker 之间的动作、失败语义和验收合同，再补宿主红测；烧录仍需单独确认目标设备和授权。

## 2026-08-22 · Host Action 课程复跑完成工程与板型只读核对 · Codex
- 做了什么：从与 `origin/main` 一致的 `34087cd` 干净基线读取 Maker 规则、工程结构、配置解析、按键扫描、动作解释、事件分发和 USB／BLE 输出入口，并用 `easyinput-board-cy` 核对 EasyInput V2.0／`v2`／AI Keyboard V2.1 的同一板型映射和硬件边界。
- 为什么这么做：先建立可定位的原项目起点，避免把 App 策略、通信协议或旧试跑结果误当成板级事实。
- 怎么理解：实体按键由 `gpio_keys` 产生去抖后输入，`app_main` 查询 `ConfigState` 中的 `Keymap`，`keymap` 将动作转为固件事件，再由运输选择交给 USB 或 BLE。当前只证明源码声明和入口可定位，不代表构建、烧录或真机行为已验证。
- 产出（路径）：`flow/tasks/T01-工程结构与板型只读核对.md`；主要源码指针已写入该任务卡。
- 问题→解决：板型扫描器因未选择条件编译分支，对 `board_pins.h` 给出两个 `WARN` 而非 `FAIL`；继续只读核对顶层 CMake 的 `v2`，组件 `EASY_INPUT_BOARD_V2=1`，以及 `board_pins.h` 声明，未发现冲突。
- 下一步：保持现有边界，按 Maker 规则运行全部宿主测试，再用锁定的 ESP-IDF 5.5.5 完成默认构建；不烧录。

## 2026-08-22 · Maker README 页尾补回公众号二维码 · Codex
- 做了什么：从公开仓库 `project-flow-cy` 的固定提交原样复制既有公众号二维码，在 Maker README 页尾增加“关注公众号”区块，并补齐来源、文件校验值和 GPL-3.0-or-later 许可证文本；未修改固件、测试或硬件配置。
- 为什么这么做：Maker 仓库首页遗漏了已经公开使用的公众号入口；直接复用既有资产可保持品牌入口一致，避免重新生成或改写二维码。
- 怎么理解：二维码是 README 的独立视觉资产，继续遵守其来源仓库的 GPL-3.0-or-later；Maker 根目录其他材料的许可证不变。本次只是文档与公开资产完善，不代表固件功能或真机状态发生变化。
- 产出（路径）：`README.md`、`assets/readme/wechat-qr.jpg`、`THIRD_PARTY_NOTICES.md`、`LICENSES/GPL-3.0-or-later.txt`；本地预览位于被 Git 忽略的 `assets/readme/page/`。
- 问题→解决：两个仓库的根许可证不同，不能只复制图片而省略来源；通过固定源提交、核对原文件 SHA-256、保留完整 GPL 文本和公开 notice 解决。预览工具要求输出目录位于仓库内，因此改在已忽略目录生成，桌面 1440 px 与手机 390 px 完整页面均通过人工检查。
- 下一步：维护者审阅本次 README、资产和许可证差异；如需提交或推送，另行明确授权，不能把当前工作区其他未提交改动一并带入。

## 2026-08-22 · Maker 编码器原生文字选择软件侧完成 · Codex
- 做了什么：先用对照现象确认只有编码器 `cursor + text_caret_select` 配置会被 `unknown_action` 拒绝，再按现有 EasyInput 配置合同补宿主红测和完整实现。Maker 现在兼容规范名称与旧名称，短按切换文字选择，旋转发送标准键盘 Shift+左右方向键，3 秒长按保留既有系统配置语义；同时加入队列背压、真实键盘快照和 USB/BLE 连接 owner 保护。未修改 App、Host Action 协议、硬件配置、分区或设备身份，未烧录。
- 为什么这么做：普通滚动配置能够同步，已经排除 BLE 连接、配置接收和 Host Action 通道；缺口是 Maker 没有实现 App 已发送的编码器文字选择动作，补齐完整语义才能让新手按教程同步整份配置，而不是绕开该动作。
- 怎么理解：第一次短按进入选择，顺／逆时针分别产生完整 `Shift+ArrowRight/Left`，第二次短按退出；默认仍是 `scroll_axis_toggle`。旧名称只作为兼容入口，不产生另一套行为。
- 产出（路径）：任务合同与验收位于 `flow/tasks/T05-编码器原生文字选择兼容.md`；实现位于 `components/keyboard/` 与 `main/`，宿主测试位于 `host_test/`。
- 测试与构建：实现前 CMake 因缺少 `encoder_press_gesture.cpp` 产生真实红灯；随后从 Git 暂存区导出仅含编码器改动的隔离快照，定向 6/6、完整宿主 56/56 通过。该快照用 ESP-IDF 5.5.5 对 `v2` / `esp32s3` 默认构建成功，应用镜像为 `0x18fd00` 字节，3 MiB App 分区剩余 `0x170300` 字节（48%）；未暂存的 Host Action 改动没有参与，`git diff --cached --check` 通过。
- 问题→解决：源码契约测试最初仍查找旧的 `void handle_input_event`，而新实现需要 `bool` 返回值表达队列是否接收；同步测试合同后定向回归全部通过。
- 下一步：先用 EasyInput App 重新同步原先失败的 `cursor + text_caret_select` 配置并观察成功确认；只有需要验证板上行为时，再单独确认是否烧录当前新镜像。构建成功不能替代这两项证据。

<!-- 新记录模板：
## YYYY-MM-DD · <任务> · <执行者>
- 做了什么：
- 为什么这么做：
- 产出（路径）：
- 下一步：
-->
